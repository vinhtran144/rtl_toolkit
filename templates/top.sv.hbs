`timescale 1ns / 1ps

{{#if hasPkg}}import {{project_name}}_pkg::*;
{{/if}}

{{#eq file_type "if"}}
interface {{project_name}}_if #(`include "{{module_param}}")(`include "{{module_sign}}");
    {{#each logic_header}}`include "{{this}}"
    {{/each}}

    {{#each task_header}}`include "{{this}}"
    {{/each}}

endinterface : {{project_name}}_if

{{else}}
module {{projectName}} #(`include "{{module_param}}")(`include "{{module_sign}}");
    {{#each logic_header}}`include "{{this}}"
    {{/each}}

    {{#each task_header}}`include "{{this}}"
    {{/each}}
endmodule: {{project_name}}
{{/eq}}